/**
 * Vitest setup for `apps/ui` — registers `@testing-library/jest-dom` matchers so
 * UI tests can assert via `toBeInTheDocument()`, `toHaveClass()`, etc.
 */
import '@testing-library/jest-dom/vitest';

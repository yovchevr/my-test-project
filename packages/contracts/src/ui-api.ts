/**
 * UI <-> API boundary contract (FR-010, FR-021).
 *
 * Concrete shapes (`UiApiAnswerContract`, `UiApiSearchRequestContract`, ...) land in
 * STORY-002. This file is intentionally empty so the workspace scaffold can be wired
 * end-to-end (STORY-001) without prejudging the schema.
 */
export {};
